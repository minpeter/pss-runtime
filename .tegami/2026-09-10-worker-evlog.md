---
packages:
  npm:@minpeter/pss-coding-agent:
    replay:
      - exit-prerelease(npm:@minpeter/pss-coding-agent)
---

## Tooling

Update the private Worker application's evlog dependency to 2.28.1 while retaining its caret range. This private tooling change does not bump a published package.
