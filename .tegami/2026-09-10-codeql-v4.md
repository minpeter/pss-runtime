---
packages:
  npm:@minpeter/pss-runtime:
    replay:
      - exit-prerelease(npm:@minpeter/pss-runtime)
---

## Update paired CodeQL actions

Update CodeQL init and analyze together to SHA-pinned v4.37.9 and reject mismatched action references in workflow validation.
