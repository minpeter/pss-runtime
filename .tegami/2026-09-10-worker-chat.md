---
packages:
  npm:@minpeter/pss-coding-agent:
    replay:
      - exit-prerelease(npm:@minpeter/pss-coding-agent)
---

## Tooling

Update the private Worker application's Chat SDK to 4.40.0 while preserving the declared Telegram and memory-state adapter versions. This private tooling change does not bump a published package.
