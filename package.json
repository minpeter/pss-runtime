{
  "name": "pss-next",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "node --conditions=@minpeter/pss-source --import tsx examples/basic.ts",
    "dev:tui": "node --conditions=@minpeter/pss-source --import tsx packages/coding-agent/src/tui.ts",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck && tsc -p tsconfig.json --noEmit",
    "test": "turbo run test && vitest run scripts/verify-release-artifacts.test.mjs",
    "lint": "ultracite check .",
    "format": "ultracite fix .",
    "check:runtime-api": "node scripts/check-runtime-api.mjs",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "pnpm build && changeset publish",
    "verify:release": "pnpm check:runtime-api && node scripts/verify-release-artifacts.mjs"
  },
  "devDependencies": {
    "@minpeter/pss-coding-agent": "workspace:*",
    "@minpeter/pss-runtime": "workspace:*",
    "@biomejs/biome": "2.4.15",
    "@changesets/cli": "^2.29.7",
    "@types/node": "latest",
    "tsx": "latest",
    "turbo": "^2.6.1",
    "typescript": "latest",
    "ultracite": "7.7.0",
    "vitest": "^4.1.6"
  }
}
