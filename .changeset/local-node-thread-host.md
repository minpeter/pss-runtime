---
"@minpeter/pss-runtime": patch
---

Add a Node platform adapter with a file-backed thread host and move Cloudflare internals under `src/platform/cloudflare` while keeping the public Cloudflare subpath stable.
