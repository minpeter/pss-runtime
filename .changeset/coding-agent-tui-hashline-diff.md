---
"@minpeter/pss-coding-agent": minor
---

Add the hashline edit_file flow with a TUI diff viewer: anchored diff sections, senpi-style word-level highlights (faint region tints, intra-token inverse marks, dim context rows for unchanged lines, hunks sorted by line number), C0 control-character sanitization in the read/diff render paths, and a code-flow module layout under `src/tui/`.
